# Copy to .env and adjust as needed
# For Docker runtime
NODE_ENV=production
PORT=3200
DATABASE_URL=file:/data/dev.db
JWT_SECRET=please_change_this
PUBLIC_APP_URL=http://localhost:3200

# For local dev (server/.env is already set for SQLite in project dir)